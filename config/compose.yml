services:
  app:
    build:
      context: ../
      dockerfile: config/Dockerfile
    container_name: blood_app
    env_file:
      - app.env
    expose:
      - "8000"

networks:
  deploy_net:
    external: true
